import React from 'react';
import Stopwatch from './Stopwatch';

let stopwatchContainer = (props) => {

    return (
        <div>
            <Stopwatch />
        </div>
    )
}

export default stopwatchContainer;